# Web framework
fastapi~=0.115.12
uvicorn[standard]~=0.34.0
python-multipart~=0.0.20      # OAuth2PasswordRequestForm (form-data) icin gerekli

# Validation / settings
pydantic~=2.11.4
pydantic-settings~=2.9.1
email-validator~=2.2.0        # pydantic EmailStr icin gerekli

# Database
SQLAlchemy~=2.0.40
alembic~=1.15.2
asyncpg~=0.30.0               # PostgreSQL async driver
greenlet~=3.1.1               # SQLAlchemy async calisma zamani gereksinimi

# Auth
PyJWT~=2.10.1
passlib~=1.7.4
bcrypt~=4.0.1                 # DIKKAT: passlib 1.7.4 bcrypt >= 4.1 ile uyumsuz,
                              # yukseltilirse register 500 verir.

# Hiz siniri deposu (istege bagli: yalnizca REDIS_URL verilirse
# kullaniliyor; verilmezse surec ici bellek). redis.asyncio icin.
redis~=6.4.0

# DI
dependency-injector~=4.46.0
